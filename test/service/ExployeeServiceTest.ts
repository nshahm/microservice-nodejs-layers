import  {expect } from "../config/Testing";
import { EmployeeService } from "../../src/service/EmployeeService";

describe("Employee API and service testcases", ()=> {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });

    it("invoke employee API", (done) => {
        done();
    });
});

    // beforeEach((done) => {
    //     // mockgoose.reset();
    //     // DAO instance 
    //     employeeDAO = getInstance<IEmployeeDAO>("IEmployeeDAO");

    //     // // mocking EmployeeModel
    //     // mockModel = sinon.mock(EmployeeModel);
    // });